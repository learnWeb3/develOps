<% if(flash && flash.message && flash.type){ %>
<div class="alert alert-<%= flash.type %> alert-dismissible fade show position-absolute top-0 end-0" role="alert">
    <%= flash.message %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
</div>
<% } %>